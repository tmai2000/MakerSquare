function bouncer (age) {
    var text;
    if(age >= 21 && age < 100){
        text="Welcome";

    }else if(age < 21 && age > 1){
      text="Go Away";
    }else{
        text="Stop Playing Around";
    }
    return text;
}